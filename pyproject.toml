[project]
name = "sdata-testdata"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = []

[dependency-groups]
dev = [
  "pytest>=8.0.0",
]
docs = [
  "mkdocs>=1.6.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
